---
interface Props {
  chart: string;
}

const { chart } = Astro.props;
---

<div class="mermaid not-typography">{chart}</div>

<script>
  import mermaid from "mermaid";
  mermaid.initialize({ startOnLoad: true });
</script>
